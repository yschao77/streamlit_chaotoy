---
description: Google Drive 與 Excel/xlsm 讀寫：不新建檔、保留 VBA、條碼文字
globs: "**/*.py"
alwaysApply: false
---

# Google Drive 與 Excel

## 連線與檔案 ID

- Service：`st.secrets["textkey"]`，CLI / GitHub Actions 用 `GOOGLE_SERVICE_ACCOUNT_JSON`
- 資料夾常數與來源目錄在 `utils.py` 的 `TRACKED_SOURCES`。**禁止 API 新建檔**（含 `files.create` / `files.copy`）；service account 沒有 Drive 配額，只能 `update` 已存在的檔。Sitegiant_UPC 請先手動放 `batch_edit_upc_added_only.xlsx`，沒有 `existing_file_id` 就停止。
- 新資料夾必須把 service account（`textkey` 的 `client_email`）加為檢視者，否則 `files.list` 會是空的。
- 選最新檔用 `pick_latest_gdrive_file`（解析檔名日期，不要只靠檔名排序）。Sitegiant 來源可含 zip，用 `download_source_spreadsheet` 解出 xlsx。

| 常數 | 用途與檔名 |
|------|-----------|
| `ID_SITEGIANT_UPC_FOLDER` | `batch_edit_item_upc_assignment_all_DD-MM-YYYY-*.xlsx`（或同名 zip） |
| `ID_SITEGIANT_BATCH_FOLDER` | `batch_edit_basic_info_all_DD-MM-YYYY-*.xlsx`（或同名 zip） |
| `ID_SHOPEE_MASS_UPDATE_FOLDER` | `mass_update_sales_info_3062950_YYYYMMDD*.xlsx` |
| `ID_PREORDER_ORDERS_FOLDER` | `Orders_DD-MM-YYYY-*.xlsx` |
| `ID_PREORDER_TRACKER` | `預購追蹤.xlsx`（只覆寫此 file id） |
| `ID_SG_RESTOCK_TEMPLATE` | Import Restock 官方空殼（只讀，禁止 update） |

## 讀寫引擎

- 只讀可用 calamine；寫回 xlsm：`keep_vba=True`
- 條碼欄寫入前 `clean_barcode`，儲存格 `@`

## 工作表與對鍵

| 檔案 | 工作表 |
|------|--------|
| 麗嬰採購產品總表.xlsm | `麗嬰國際產品總表`、`已處理採購單`、`刪除紀錄`、`metadata` |
| 蝦皮賣場商品列表.xlsm | `蝦皮商品列表`、`匯入檔案` |
| 商品蝦皮麗嬰價格統整表.xlsx | `商品蝦皮麗嬰價格統整表` |

- 內部 `自定義編碼` ↔ 蝦皮 `iSKU`；內部 `c` ↔ 麗嬰 `條碼`
- `c` ↔ batch_edit `國際條碼（UPC）` → 統整表 `sitegiant庫存SKU` = `庫存貨品名稱`
- 入庫單「庫存貨品名稱」抓 `sitegiant庫存SKU`，不要用蝦皮商品名稱
- 蝦皮校正來源為最新 mass_update；匯入去重用 md5
