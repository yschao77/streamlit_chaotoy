---
description: 預購追蹤業務規則：工作表寫入語意、結單／到貨、Drive、看板口徑
globs: views/preorder_page.py,utils.py
alwaysApply: false
---

# 預購追蹤（streamlit_chaotoy）

## 入口與檔案邊界

- 不接 SiteGiant Open API、不接綠界 API。Excel／Drive 進出；金流／物流只在 SiteGiant 後台開。
- 第四主模組「🗓️ 預購追蹤」→ 子頁：總覽／建檔／對訂單／結單／到貨催款（`views/preorder_page.py`）。邏輯在 `utils.py`。**不要再掛回 Sitegiant。**
- `預購追蹤.xlsx`（`ID_PREORDER_TRACKER`）須先手動建檔再覆寫（禁止 API 新建）。與 `預購結單紀錄.xlsx`（`ID_PREORDER_VENDOR_LOCK_ARCHIVE`）是不同檔，禁止把已填訂量備份寫進追蹤主檔。

## 工作表寫入語意（改存檔前必對）

| 工作表 | 寫入方式 | 禁止 |
|--------|----------|------|
| **預購訂單** | 最新 All Orders 預購列的**整表快照**；每次重抓＋覆寫雲端即**整表蓋掉**舊內容 | 當成歷史流水帳追加；誤以為是官方「待處理」 |
| **SKU狀態** | **upsert**（新 SKU 補上；已「到貨」保留） | 整表清空重寫後弄丟到貨狀態 |
| **通知紀錄** | 只存訂單編號＋SKU＋首次通知日；`stamp_preorder_first_notify` **不覆寫**既有首次通知日 | 每次存檔重算蓋掉通知日 |
| **活動表／廠商單歷史** | 依畫面編輯後寫回 | 與結單紀錄彙整檔混寫 |

權威來源：看板／催款對齊**當下** All Orders；長期要留的是通知日與到貨狀態，不是舊匯出殘單。

## 看板與狀態

- 預購列：商品名稱含「預購」（不過濾商城）。對鍵庫存SKU（空白才商品SKU）。
- 看板兩欄：已付款／未付款。不看付款方式、不拆貨到付款。官方狀態說明：https://support.sitegiant.com/zh/knowledge-base/how-the-order-payment-and-fulfillment-status-works-zh/
- 「預購／到貨」**只在「到貨催款」可編**；「對訂單」唯讀。標到貨表只列狀態＝預購**且已接單（客戶量）＞0**。
- 催款：到貨 × 未付款 × 非已取消 × 非已退款。可打單：到貨 × 已付款 × 訂單狀態＝待處理。催款「已通知」一按即覆寫雲端，按鈕文字須含「且覆寫雲端預購追蹤」。

## 建檔／結單

- 結單日：活動表用 **DatetimeColumn** 挑選器；寫回／驗證字串 `YYYY-MM-DD HH:mm`。有 SKU 必填。**僅建檔子頁可編**。
- 活動表另有「賣場後台連結」「日曆事件ID」。覆寫雲端成功後 `sync_preorder_calendar_events` upsert 指定日曆（`PREORDER_CALENDAR_ID`）；**同廠商檔名只一筆事件**；失敗不回滾 Drive。提醒：共用日曆上 API `reminders` 只對 SA 私有，未關閉事件用 `useDefault: true`，成員須在該日曆設「活動通知 2 小時前」。組內全實際關閉→標題 `[已結]…`；組內皆無合格結單日→刪事件。
- 廠商單匯入活動表（可無條碼；xlsx／xls）。upsert：條碼 → 檔名+貨號 → 檔名+品名。不覆蓋 SKU／自購／上限。已實際關閉列不覆寫貨號／品名／條碼。
- **建碼：** 建檔頁「送交待審核」審核表（空 SKU）可改 L2／L3／L4／**產品關鍵字** → GAS batch（狀態＝預購）→ 回填 SKU、`SKU審核=待審核`。secrets 頂層 `SKU_SUBMIT_*`（勿放 `[textkey]` 內）；Web App `ANYONE_ANONYMOUS`＋`BATCH_API_TOKEN`。見 `secrets.mdc`／assistant `sku-submit.mdc`。
- 已關閉列鎖自購／上限／SKU／月份／結單日；清空實際關閉＝重開。賣場後台連結關閉後仍可改。
- 鎖定數量＝`自購 + min(客戶量, 上限)`。未勾選／已關閉不重算。回填只改訂量（先條碼再貨號）。SiteGiant 採購單空殼只本機下載，禁止覆寫 `ID_SG_RESTOCK_TEMPLATE`。
- 結單同步：優先 update 同名 `{stem}_結單紀錄.xlsx` 或彙整檔 `ID_PREORDER_VENDOR_LOCK_ARCHIVE`；`files.create` 僅次要。檔名剝 `_訂量` 再加 `_結單紀錄`。
- All Orders 讀 `ID_SITEGIANT_ALL_ORDERS_FOLDER` 最新 `Orders_DD-MM-YYYY-*.zip`，解成 xlsx。

## 待做／刻意不做（勿當缺口亂補）

- **已完成：** 結單日 × Google 日曆（覆寫雲端後 upsert；見計畫 `結單日_google_日曆`）。
- **已實作：** 建檔「送交待審核」→ GAS batch（含產品關鍵字）；非匯入即靜默建碼。
- **未拍板前不動工：** 入庫分配（第一版仍 SiteGiant）、商品清單帶廠商貨號、預購訂單歷史流水帳。
