---
description: 預購追蹤頁面 UX 改善原則：生命週期看板、存檔語意、勾選防呆、催款操作
globs: views/sitegiant_page.py
alwaysApply: false
---

# 預購追蹤 UX 原則

改 `sitegiant_page.py` 的 `_render_preorder_*` 系列前，優先檢查新改動有沒有讓下列已知痛點變更糟：

1. **生命週期要可視化**：活動應依 `實際關閉`＋`SKU狀態`（預購/到貨）呈現目前所在階段（收單中／已結單待到貨／催款中／完成），不要只靠使用者從五段散落資訊自己拼。新增欄位或狀態時，優先想「這個要不要反映到階段總覽」。
2. **存檔語意一致**：同一頁不要一半自動存雲端、一半要手動按「覆寫雲端預購追蹤」。新按鈕若會改資料，明確標示是否已寫回雲端；避免使用者猜。
3. **重新載入按鈕語意要清楚**：若新增另一顆重新載入/清除 session 的按鈕，要寫清楚它清了什麼、不影響什麼，不要讓兩顆按鈕字面相近但影響範圍不同。
4. **關鍵上傳檔案不要只存在 session**：結單等一次性關鍵動作依賴的檔案（如廠商訂購單），鎖定後應把結果隨鎖定寫回雲端／持久化，不要讓「重新整理頁面」變成必須重新翻找信箱附件。
5. **勾選表預設值要與操作建議一致**：若文件/註解說「不要整份全選」，`CheckboxColumn` 就不要 `default=True`；列數多時提供全選/取消全選/依條件勾選，不要逐列手動勾。
6. **鎖定規則要在互動當下擋，不要事後還原再跳訊息**：已關閉列等不可改欄位，優先用 `column_config` 逐列/逐欄 `disabled`；做不到才退回「送出後還原＋st.info」，且訊息要指到是哪一格、哪個原因。
7. **催款/名單類文字輸出要支援單筆操作與穩定複製**：`st.text_area` 的 `key` 不要隨內容 hash 變動（游標會重置）；批次動作（如「本次催款已通知」）旁應保留可對單筆操作的路徑。
8. **避免五段永遠全展開**：新增段落或大量資料時，優先讓「與目前階段無關的列」預設收進摺疊區，不要讓使用者永遠看到整頁所有歷史列。
9. **表格／清單一律假設列數會持續變多**：重點先列（要處理／要鎖定／要通知的列排前面），說明性或跳過的列放後面或收進 `st.expander`；不要讓使用者滑過大量「與本次操作無關」的列才看到重點（例：`_show_preorder_lock_tables` 要鎖定的列先顯示，skipped 收進摺疊區）。大量列需要搜尋／篩選時，跟「目前階段」的畫面重組一起設計，不要分次零散加，避免兩套篩選邏輯打架。
10. **逐項明細不要用「每個項目一個 `st.expander`」**：項目數（SKU／活動）一多，畫面高度會隨項目數線性變長、渲染變慢。優先做法是「彙總表（可搜尋）＋ 選取才渲染明細」，明細區塊數量只跟使用者這次選了幾個有關，跟項目總數無關（例：`_render_preorder_sku_overview_and_detail` 用 `st.multiselect` 選 SKU 才顯示合併明細，取代舊版逐 SKU expander）。同一份資料如果會用不同口徑呈現多次（彙總數字 vs 逐行原始資料），要讓使用者分得出兩者口徑可能不同，不要無說明地並排兩張數字對不上的表。
11. **階段總覽的數字要濾掉「技術上符合條件、但根本沒有實際訂單」的列**：只靠 `實際關閉`＋`SKU狀態` 兩個欄位判斷階段會把「結單了但沒人下單」的 SKU 也算進「已結單待到貨」，讓數字虛高、逼使用者確認一堆其實不用處理的列。等有 All Orders 資料時，要用實際接單件數（例：`sku_accepted_qty_map(board)`）再篩一次，件數為 0 的 SKU 不列入該階段；還沒載入訂單資料的那一輪（例如頁面剛開、`accepted_qty_by_sku` 還是 `None`）維持全部列出，並用 caption 講清楚「這裡還沒濾過，載入 All Orders 後會自動補篩」——不要在沒把握時就靜默濾掉，也不要永遠不濾（見 `_render_preorder_lifecycle_overview`）。**同一個「階段」概念如果不只一處在算**（例如頂部「生命週期總覽」跟「3. 對客戶訂單」SKU 彙總表都各自呼叫 `compute_preorder_campaign_stages`），濾掉沒訂單的「已結單待到貨」要每個地方都同步處理，只改一處會讓使用者在不同段落看到同一顆 SKU 呈現不一致的階段（`_render_preorder_sku_overview_and_detail` 用 relabel 成空字串而非整列隱藏，因為那張表還要讓人管理／改「狀態」）。
12. **不要靠 `st.empty()` 加重新進入 `.container()` 來「原地替換」複合式（多元件）內容**：同一次 script run 內對同一個 placeholder 寫入巢狀內容（`st.columns`＋`st.metric`＋`st.expander` 這種組合）後，就算中間插了 `ph.empty()` 再 `with ph.container():` 第二次，實測（Streamlit 1.51）並不保證清掉前一次的畫面，會兩份都殘留、只有其中一份數字是對的（很難肉眼發現，因為多數欄位剛好兩次數字相同）。需要「先顯示保守版、資料到位後補準」的畫面，正確做法是**只渲染一次**：渲染前就把依賴資料準備好（例如提早 `load_preorder_orders()` 並存進 `st.session_state` 快取，`_render_preorder_orders_board` 之後撿到快取就不會重打 API），不要企圖在同一輪 script run 內對同一個 widget 區塊做兩次「畫→清→再畫」。

新增 UI 前先讀 [`.cursor/rules/preorder-tracking.mdc`](.cursor/rules/preorder-tracking.mdc)（業務規則）與 [`docs/manuals/user/pages/13-preorder-tracker.md`](docs/manuals/user/pages/13-preorder-tracker.md)（操作手冊），確認新畫面沒有製造新的「常見問題」。
