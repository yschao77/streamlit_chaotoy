---
description: 預購追蹤頁面 UX：生命週期、存檔語意、子頁、結單日挑選器、標到貨篩選
globs: views/preorder_page.py
alwaysApply: false
---

# 預購追蹤 UX 原則

改 `preorder_page.py` 的 `_render_preorder_*`／子頁 `render` 前，優先檢查新改動有沒有讓下列已知痛點變更糟：

1. **生命週期要可視化**：活動應依 `實際關閉`＋`SKU狀態`（預購/到貨）呈現目前所在階段（收單中／已結單待到貨／催款中／完成），不要只靠使用者從各子頁散落資訊自己拼。新增欄位或狀態時，優先想「這個要不要反映到總覽」。
2. **存檔語意一致**：頁面預設手動存（要按「覆寫雲端預購追蹤」才寫回）。唯一例外是催款「已通知」——怕漏存催款紀錄，維持點一下就存，但按鈕文字必須寫出「且覆寫雲端預購追蹤」，不要無聲自動存；之後新增類似「一按就存」的按鈕要比照這個命名。
3. **重新載入按鈕語意要清楚**：若新增另一顆重新載入/清除 session 的按鈕，要寫清楚它清了什麼、不影響什麼，不要讓兩顆按鈕字面相近但影響範圍不同。
4. **關鍵上傳檔案不要只存在 session**：結單等一次性關鍵動作依賴的檔案（如廠商訂購單），鎖定後應把結果隨鎖定寫回雲端／持久化。階段6：已填訂量在 `preorder_vendor_fill` 首次算出時同步一次——優先 update 既有同名結單檔或彙整檔 `ID_PREORDER_VENDOR_LOCK_ARCHIVE`；不要每次先 `create` 撞 SA 配額。
5. **勾選表預設值要與操作建議一致**：若文件/註解說「不要整份全選」，`CheckboxColumn` 就不要 `default=True`；列數多時提供全選/取消全選/依條件勾選。逐筆催款表：`勾選` 預設＝尚無首次通知日（已通知列預設不勾）；全選仍可刻意全勾。
6. **鎖定規則要在互動當下擋，不要事後還原再跳訊息**：已關閉列等不可改欄位，優先用 `column_config` 逐列/逐欄 `disabled`；做不到才退回「送出後還原＋st.info」，且訊息要指到是哪一格、哪個原因。
7. **催款/名單類文字輸出要支援單筆操作與穩定複製**：`st.text_area` 的 `key` 不要隨內容 hash 變動（游標會重置）；批次動作（如「本次催款已通知且覆寫雲端預購追蹤」）旁應保留可對單筆操作的路徑。
8. **避免無關工作區永遠全展開**：入口已拆成總覽／建檔／對訂單／結單／到貨催款；新增段落或大量資料時，優先讓「與目前子頁無關的列」預設收進摺疊區。
9. **表格／清單一律假設列數會持續變多**：重點先列（要處理／要鎖定／要通知的列排前面），說明性或跳過的列放後面或收進 `st.expander`。結單回填：`campaign_unmatched`（已鎖定對不到廠商檔）才黃字警告；`vendor_unmatched`（未鎖定／未開放預購）收進摺疊「可忽略」，不要跟真問題混成一張警告表。
10. **逐項明細不要用「每個項目一個 `st.expander`」**：項目數（SKU／活動）一多，畫面高度會隨項目數線性變長、渲染變慢。優先做法是「彙總表（可搜尋）＋ 選取才渲染明細」，明細區塊數量只跟使用者這次選了幾個有關，跟項目總數無關（例：`_render_preorder_sku_overview_and_detail` 用 `st.multiselect` 選 SKU 才顯示合併明細）。同一份資料如果會用不同口徑呈現多次（彙總數字 vs 逐行原始資料），要讓使用者分得出兩者口徑可能不同，不要無說明地並排兩張數字對不上的表。
11. **階段總覽的數字要濾掉「技術上符合條件、但根本沒有實際訂單」的列**：只靠 `實際關閉`＋`SKU狀態` 兩個欄位判斷階段會把「結單了但沒人下單」的 SKU 也算進「已結單待到貨」。等有 All Orders 資料時，要用實際接單件數（例：`sku_accepted_qty_map(board)`）再篩一次，件數為 0 的 SKU 不列入該階段；還沒載入訂單資料的那一輪維持全部列出，並用 caption 講清楚「請到對訂單載入後會自動補篩」。**同一個「階段」概念如果不只一處在算**（總覽跟「對訂單」彙總表），濾掉沒訂單的「已結單待到貨」要每個地方都同步處理；**改「預購／到貨」只在「到貨催款」**。
12. **不要靠 `st.empty()` 加重新進入 `.container()` 來「原地替換」複合式（多元件）內容**：同一次 script run 內對同一個 placeholder 寫入巢狀內容後再清再畫，實測並不保證清掉前一次。需要「先顯示保守版、資料到位後補準」的畫面，正確做法是**只渲染一次**：渲染前就把依賴資料準備好（例如提早 `load_preorder_orders()` 並存進 `st.session_state`），不要企圖在同一輪對同一個 widget 區塊做兩次「畫→清→再畫」。
13. **SKU「預購／到貨」狀態唯一可編輯處在「到貨催款」**：「對訂單」看板只唯讀顯示狀態；標到貨編輯表只列狀態＝預購**且已接單（客戶量）＞0**（零接單／空白不列）；標到貨與催款／可打單同頁。通知紀錄畫面排除已結清列，勿直接 `st.dataframe` 原始三欄工作表充當待辦。
14. **子頁共用 session 真相來源**：活動表／SKU 狀態／All Orders 快取跨子頁共用；總覽跳轉用 `preorder_nav_target` → `app.py` 設 `preorder_sub_page` 後 rerun，不要另做一套導覽。
15. **結單日用 DatetimeColumn 挑選器**：活動表不要靠手打 `YYYY-MM-DD HH:mm`；寫回／驗證前格式化成該字串。有 SKU 仍必填結單日。

新增 UI 前先讀 [`.cursor/rules/preorder-tracking.mdc`](.cursor/rules/preorder-tracking.mdc)（業務規則）與 [`docs/manuals/user/pages/13-preorder-tracker.md`](docs/manuals/user/pages/13-preorder-tracker.md)（操作手冊），確認新畫面沒有製造新的「常見問題」。
