---
description: 專案定位、檔案職責、領域詞彙；勿改 temp/ 與 Secrets
alwaysApply: true
---

# streamlit_chaotoy

Streamlit 進銷存工具：「麗嬰與蝦皮商務數據情報中心」。資料在 Google Drive 的 Excel/xlsm，不在本機資料庫。

## 檔案職責

- `app.py`：`page_config`、側邊欄路由；把 `gdrive_cfg` / 檔案 ID 傳給子頁
- `utils.py`：Drive I/O、來源目錄 `TRACKED_SOURCES`、條碼清洗、主表/蝦皮 xlsm 讀寫的**唯一實作**
- `views/sitegiant_page.py`：Sitegiant 入庫轉換、歷史入庫、批量 UPC、預購追蹤
- `views/integration_page.py`：麗嬰/蝦皮/內部商品三表整合與審核
- `views/status_page.py`：各雲端試算表最新修改時間
- `temp/`：舊版 `app_*.py` 快照，**只讀、禁止修改或當實作來源**

## 領域詞彙

- **麗嬰**：採購/批發主檔（`麗嬰採購產品總表.xlsm`）
- **蝦皮**：賣場商品列表（`蝦皮賣場商品列表.xlsm`）
- **Sitegiant**：電商入庫單格式
- **三表整合**：內部商品列表 × 蝦皮 × 麗嬰 → `商品蝦皮麗嬰價格統整表`

## 硬限制

- 憑證只走 `st.secrets["textkey"]`，禁止寫入 repo 或印出金鑰
- 新邏輯放 `utils.py` 或 `views/`，不要再把整份 app 複製進 `temp/`
- 商業數字（稅率 1.05、零售八折 0.8、UID 後備 3473）未經要求不要改
