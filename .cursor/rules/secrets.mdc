---
description: 憑證與 git：禁止把金鑰寫進 repo、workflow 或畫面
alwaysApply: true
---

# Secrets and git

This GitHub repo is public. Credentials never go in commits, YAML, issues, or UI.

## Where keys live

- Streamlit：本機 `.streamlit/secrets.toml`（已在 `.gitignore`）
  - Service account：`[textkey]` table → `st.secrets["textkey"]`
  - SKU 送審：**頂層**鍵（必須在任何 `[table]` **之前**）
    - `SKU_SUBMIT_WEBAPP_URL` = Web App `/exec` URL
    - `SKU_SUBMIT_BATCH_TOKEN` = 與 GAS Script Property `BATCH_API_TOKEN` 相同
- GitHub Actions / CLI：repo Secret `GOOGLE_SERVICE_ACCOUNT_JSON`，用環境變數讀取
- Drive 資料夾 ID 可以寫在 `app.py`；**service account JSON / private_key 不行**

### SKU secrets 踩坑（已遇過）

```toml
# ❌ BAD — 寫在 [textkey] 後面／裡面；TOML 會算進 textkey，st.secrets["SKU_SUBMIT_…"] 讀不到
[textkey]
type = "service_account"
# …
SKU_SUBMIT_WEBAPP_URL = "https://script.google.com/…/exec"

# ✅ GOOD — 頂層鍵放在 [textkey] 之前
SKU_SUBMIT_WEBAPP_URL = "https://script.google.com/…/exec"
SKU_SUBMIT_BATCH_TOKEN = "…"

[textkey]
type = "service_account"
```

- 改 secrets 後須**重啟** Streamlit。
- Web App 若 `access: MYSELF`，Streamlit 會收到 HTTP 401＋Google 登入 HTML。須 `ANYONE_ANONYMOUS`＋`BATCH_API_TOKEN` 對齊。

```yaml
# ❌ BAD — JSON 寫死在 workflow（會公開）
env:
  GOOGLE_SERVICE_ACCOUNT_JSON: '{"type":"service_account","private_key":"..."}'

# ✅ GOOD
env:
  GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
```

```python
# ❌ BAD
print(os.environ.get("GOOGLE_SERVICE_ACCOUNT_JSON"))
st.write(st.secrets["textkey"])

# ✅ GOOD
info = load_google_service_account_info()  # 只在記憶體使用，不要印出
```

## Do not commit

Respect `.gitignore`. Never `git add -f` these:

- `.streamlit/secrets.toml`、`.env`、`*service-account*.json`、`*.pem`
- 本機匯出 `batch_edit_basic_info_all_*.xlsx`、`mass_update_sales_info_*.xlsx`

錯誤訊息只寫「憑證缺失／無效」，不要 `st.error(str(e))` 若 `e` 可能含金鑰內容。
